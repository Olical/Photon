define([
    '../core/Class'
], function(Class) {
    /*
        Class: Events

        An event managing class that can be extended by your classes.

        Requires:

            - <Class>
    */
    var Events = Class();

    return Events;
});